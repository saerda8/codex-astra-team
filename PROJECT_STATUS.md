# 项目状态

当前版本 1.6.0。

已实现角色模板、GPT-6 Astra / Sol 从 Medium 起协作、GPT-6 Luna 三档角色、实际派工通知、非阻断提醒、短指令持续工作计数、派工关联，以及候选配置和备份回滚工具。

1.4.6：取消技能对开头、过程和结论的表达限制，只在实际派工时告知交给谁、做什么；不要求重复分工。67 项离线测试通过；离线检查不代表所有客户端运行时均已验证。
1.4.7：协作派工门槛调整为 Astra Medium 起；Low 可直接完成。
1.4.8：Astra Medium 及以上的明确执行任务不论大小都先派工；普通交流和 Low 可直接处理。
1.4.9：移除项目规则中残留的小任务、纯核心任务免派条款，并防止额度优化覆盖派工要求。
1.5.0：加入 GPT-6 Sol 主会话路由与 GPT-6 Luna High / XHigh / Max 三档角色；保留 Terra，不宣称能力等价。
1.6.0：加入 GPT-6 Sol High / XHigh / Max 子角色；Astra 可派高级工程任务，Sol 6 主会话禁止自派同型号。

## 限制

- 模板模型和档位不等于运行时证据，依赖客户端和账号实际支持。
- 任务分类按文本及工具调用次数启发式判断，可能漏判或误判；因此不能拦截工具。
- 实际派工和可见说明依赖模型遵循指令；测试通过不代表所有任务自动派工。
- 不压缩主模型原始上下文，不提供 Terra 前置转发，不修改官方核心。
- 独立助手仍有指令和工具开销；token 数量不能直接换算订阅额度。
- 全局安装需要增量合并；staging 脚本主要生成项目级候选。
- 作者私有的七个 MCP workers 和 MCP 健康钩子不在本包内；技能可以独立使用。

## 演变

- 1.3.x：曾采用硬拦截，因误判和互锁废弃。
- 1.4.0：工具和结束检查改为非阻断。
- 1.4.1–1.4.2：真实档位展示、未知省略、Astra Low 起适用。
- 1.4.3：小任务直接执行、减少重复读取和协调。
- 1.4.4：修复观察状态未关联真实派工。
- 1.4.5：关键进度与最终回复展示实际分工。

公开整理仅改变分发文档与脱敏内容，不宣称额外客户端兼容性。
