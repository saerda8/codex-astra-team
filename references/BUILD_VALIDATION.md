# 验证范围

运行 python scripts/test_strategy.py -q。测试覆盖角色配置、路径边界、备份回滚、非阻断行为、短指令持续工作、派工关联及重复计数。

测试仅使用临时目录和合成事件，不调用账号或模型。真实部署还需验证模型支持、钩子信任及真实派工。历史使用发现固定文字检查、父子回合差异和短指令识别会误判，因此当前版本采用非阻断方式。

1.5.0 本地离线回归共 71 项，新增覆盖 GPT-6 Sol 主会话 Medium / Low 分流，以及 GPT-6 Luna High / XHigh / Max 三个固定角色。该结果不等于客户端已热加载或账号已完成真实派工。
